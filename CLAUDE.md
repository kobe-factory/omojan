# CLAUDE.md

ユーザーとは常に日本語で会話してください。
不明な点や、ユーザーに聞かないといけないことは全て聞いてから実装してください。
まずはplanを作成し、その後実装するように進めます。
プラン作成時の会話は必ずドキュメントにまとめてください。
このCLAUDE.mdも常に更新してください。
プランのドキュメントは別でmdファイルを作成し、CLAUDE.mdにそのファイルのパスを記載して、常に確認できるようにしてください。
いつでもこのプロジェクトの概要や文脈が会話時にわかるようにすることが重要です。
READMEも適宜更新してください。

## 概要

- おもじゃんというプロジェクトです。
- ブラウザを利用するゲームです。
- まずはPOCとして開発します。
- ログイン機能はなしです。
- ローカルなアプリケーションなので、友人５人で遊べるものにします。
- ログイン機能はないので、初回画面で自分のユーザーを選択します。画面にはユーザー５名が表示されるので、自分の名前を選択する。
- 選択したユーザーが自分のユーザーIDとなり、データが保存されていく。
- ブラウザ側にはセッションID？を作成し、ログイン状態のようにしてデータを保存していきたいが、セッションでなくてもいいです。ここは考えてください。
- URLを発行しますが、乱数をパラメータに持たせ、乱数がそのゲームのURLとして利用します。
- 1つ目のURLを発行すると、それが第１回の大会となります。
- ２つ目のURLを発行すると、それが第２回の大会となります。
- １つの大会内で複数のゲーム数があります。ゲーム数はURL発行時に決めるか、デフォルトは5回戦
- ゲームのフローは、ユーザー選択→初回のお題札の作成→作品作成画面になり、今回のお題札と手札が画面に表示され、作品を作成。前口上も入力可→全員のお題が発表され、一番おもしろい作品に投票できる→結果発表→第２回戦に移る。２回戦移行には過去の結果が見れるようにしたいので、タブ等を設けアーカイブが見れるデザインにしたい。
- フローについて、次のフローに進むタイミングは、参加ユーザー全員の入力が完了したら次のフローに進みます。ユーザーは画面をリロードすることで次のフローに進んだ画面が見れる仕組みです。１大会はずっと同じURLとします。ユーザーがリロードするor画面を開き直すアクションをするだけにします。
- 前提としてこれはLINEのグループチャットにURLを貼り付けます。ユーザーはLINEからブラウザを開き入力等します。LINEを閉じ、また開いた際に先ほど共有されたURLをまた開くので、URLは常に同じである必要があります。なので、フローの遷移等はDBに依存するため、ユーザーの行動はDBに保存する必要があります。全員がユーザーを選択した（５ユーザー）ら次の画面へ→全員がお題札を作成したら次のフローへ（この画面は大会の初戦のみ）→作品の作成画面で全員が作品を投稿したら次の画面へ→全員が一番おもしろい作品に１票入れたら次の画面へ→結果発表はこのステータスになったときの初回に開いた場合のみ出す。（クッキー等で制御）→再度URLを叩くと、２回戦の作品作成画面に移る。タブ切り替えでその大会の過去作品が見れる。
- ５人全員のステータスが完了にならないと次のフローへ進めないので、自分は投稿が完了しているが、他の人が完了していない場合は、画面に誰が入力できていないかわかるように画面に出す。◯◯のお題作成待ち, ◯◯の作品投稿待ち、　等です。
- ユーザー選択や、お題投稿など、修正したい場合があるので、全員の入力が完了するまでは修正ができるようにしてください。他の人を待っている画面のみ、戻って入力修正ができる。次のフローに進んでしまったら、戻れなくていいです。
- ユーザー選択では、１ユーザーが複数のユーザーを選択できないような仕組みが必要です。間違えて戻った場合は、間違えたユーザーは解除する必要があります。５人のユーザーが正しく５ユーザーを選択している状態にする必要があります。

## ユーザー

以下の５名のユーザーで行います。

- はじむ
- スラパン
- こんべ
- かねおか
- カズさん

上記５名ですが、一旦実装段階では、「こんべ」「カズさん」の２名でテストします。
一通り実装が完了したら５名に変更し、リリースします。

## おもじゃん解説

- おもじゃんのゲームについて解説します。
- 最初にユーザーがゲームで使用する札を作成します。例えば１ユーザー辺り２０枚の札を作成
- 札はテキストを入力して作成。
- 札には単語や短い文章を適当に記入
- POCでは画像のアップロードはなし
- 全ユーザーが札を作成するとゲーム開始
- 例えば一人２０枚作成すると、１００枚の札がそのゲームで使えます。
- １大会ではこの１００枚の札を使いまわします。
- ゲーム開始時に札がそれぞれのユーザーにランダムで１０枚配られます。
- 残りの５０枚がお題としての札として使われます。
- １大会５ゲームとします。
- 第１ゲームが開始すると、お題５０枚の札の中からランダムで１枚お題として選ばれます。
- そのお題が画面に表示され、ユーザーは手札の中から１枚札を選びます。
- お題札の頭に手札を配置するか、お題札のお尻に手札を配置するかユーザーは選べます。
- お題のテキストと手札のテキストを合体させメッセージを作ります。
- そのメッセージが一番面白かった人が勝ちとなります。
- お題＋手札は自分の作品となりますが、その作品を理解してもらうための補助テキスト（前口上）も入力できます。
- 全員のお題が出揃ったら投票へ移ります。
- 全員が５作品の中から一番面白かった作品に１票入れます。
- 全員の投票が終わったら結果発表となり、一番面白かった作品が決まります。
- 例：第１回大会の１回戦の一番面白かった作品として保存されます（他の作品もデータとして保存は必要）
- これらの作品はアーカイブとして残り、後で皆で見返せるようになります。

## 機能・デザイン

- ログイン機能はなし
- 結果発表のデータはアーカイブして後で参照できるように
- だれの投票待ちかわかるようにする
- デザインはモバイルアプリ風
- 白ベースデザイン。ゲームなのでポップに
- タイトルは「おもじゃん for 男根祭 〜ご神体ワードバトル〜」
- バージョンを記載します 初回は 「バージョン1.0」

## バージョン管理ルール

**コミット（デプロイ）のたびに必ずバージョンを更新すること。更新を忘れた場合は即座に修正コミットを行う。**

形式：`[メジャー].[機能追加].[バグ・小改善]`　各桁は独立してインクリメントし、繰り上がりはしない。

- バグ修正・小さい改善 → 第3桁をインクリメント（例：1.25.9 → 1.25.10）
- 機能追加 → 第2桁をインクリメント・第3桁はリセットしない（例：1.25.10 → 1.26.0）
- メジャーアップデート → `2.0.0`

**更新対象ファイル（4箇所）：必ず同一コミットに含める**
- `src/app/[token]/page.tsx` のフッター `v1.x.x`
- `src/app/layout.tsx` の metadata description `v1.x.x`
- `src/app/admin/page.tsx` のフッター `v1.x.x`
- `CLAUDE.md` の「現在のバージョン」

**現在のバージョン：v1.31.2**

## ローカル開発

- ポートは **3001** を使用すること
- 起動コマンド: `npm run dev -- -p 3001`
- URL: `http://localhost:3001`

## システム概要

- WEBアプリケーション
- モバイル表示前提
- モバイルアプリ風デザイン
- DBはsupabaseを利用
- vercelにデプロイ
- githubと連携し、mainにマージすると本番環境にデプロイ
- vercel,supabaseは無料枠で収めたいので問題ないか必ず確認してください。５名の閉じられたユーザーで利用するものです。

## プランドキュメント

- [docs/PLAN.md](docs/PLAN.md) — 実装プラン（常に最新に保つこと）

## システム構成

- Next.js
- Typescript
- TailwindCSS
- Vercel
- Supabase

## サービス接続

- supabaseのアカウントはあるので、接続方法をまとめてほしい
- vercelのアカウントはあるので、接続方法をまとめてほしい
- githubのアカウントはあるので、接続方法をまとめてほしい
